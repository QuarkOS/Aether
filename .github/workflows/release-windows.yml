name: Release Windows

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  windows-installer:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - name: Build shared and desktop
        run: |
          npm run build --workspace @aether/shared
          npm run build --workspace @aether/desktop
      - name: Install desktop production deps into app dir
        working-directory: apps/desktop
        run: npm install --omit=dev --no-save --install-strategy=nested
      - name: Package Windows installer
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: "false"
        run: npx electron-builder --projectDir apps/desktop --win nsis --x64 --config.npmRebuild=false
      - name: Upload installer
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          draft: false
          prerelease: ${{ contains(github.ref_name, '-') }}
          files: apps/desktop/release/Aether-Setup-*.exe
          generate_release_notes: true
          body: |
            Unsigned Windows installer. SmartScreen may warn on first run.
            Choose More info → Run anyway if Windows blocks the file.
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Upload artifact (manual dispatch)
        if: github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@v4
        with:
          name: Aether-Setup-windows
          path: apps/desktop/release/Aether-Setup-*.exe
